'''s1level103
im not writing the code the last ones, IT DOESENT SHOW ME ANYTHING
'''

import turtle
import random

artist = turtle.Turtle()
artist.pensize(3)
artist.speed(100)

def pyramid(length):
    for count in range(4):
        artist.forward(length)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
        
    for count2 in range(4):
        artist.forward(length + 1)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
         
    for count3 in range(4):
        artist.forward(length + 2)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count4 in range(4):
        artist.forward(length + 3)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count5 in range(5):
        artist.forward(length + 4)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count6 in range(4):
        artist.forward(length + 5)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count7 in range(4):
        artist.forward(length + 6)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count8 in range(4):
        artist.forward(length + 7)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count9 in range(4):
        artist.forward(length + 8)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count10 in range(5):
        artist.forward(length + 9)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count11 in range(4):
        artist.forward(length + 10)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count12 in range(4):
        artist.forward(length + 11)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count13 in range(4):
        artist.forward(length + 12)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count14 in range(4):
        artist.forward(length + 13)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count15 in range(5):
        artist.forward(length + 14)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count16 in range(4):
        artist.forward(length + 15)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count17 in range(4):
        artist.forward(length + 16)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count18 in range(4):
        artist.forward(length + 17)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count19 in range(4):
        artist.forward(length + 18)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count20 in range(5):
        artist.forward(length + 19)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count21 in range(4):
        artist.forward(length + 20)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count22 in range(4):
        artist.forward(length + 21)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count23 in range(4):
        artist.forward(length + 22)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count24 in range(4):
        artist.forward(length + 23)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count25 in range(4):
        artist.forward(length + 24)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count26 in range(4):
        artist.forward(length + 25)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count27 in range(4):
        artist.forward(length + 26)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count28 in range(4):
        artist.forward(length + 27)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count29 in range(4):
        artist.forward(length + 28)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count30 in range(4):
        artist.forward(length + 29)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count31 in range(4):
        artist.forward(length + 30)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count32 in range(4):
        artist.forward(length + 31)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count33 in range(4):
        artist.forward(length + 32)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count34 in range(4):
        artist.forward(length + 33)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count35 in range(5):
        artist.forward(length + 34)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count36 in range(4):
        artist.forward(length + 35)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count37 in range(4):
        artist.forward(length + 36)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count38 in range(4):
        artist.forward(length + 37)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count39 in range(4):
        artist.forward(length + 38)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count40 in range(5):
        artist.forward(length + 39)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count41 in range(4):
        artist.forward(length + 40)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count42 in range(4):
        artist.forward(length + 41)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count43 in range(4):
        artist.forward(length + 42)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count44 in range(4):
        artist.forward(length + 43)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count45 in range(4):
        artist.forward(length + 44)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count46 in range(5):
        artist.forward(length + 45)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count47 in range(4):
        artist.forward(length + 46)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count48 in range(4):
        artist.forward(length + 47)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count49 in range(4):
        artist.forward(length + 48)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count50 in range(4):
        artist.forward(length + 49)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count51 in range(5):
        artist.forward(length + 50)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count in range(4):
        artist.forward(length + 51)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
        
    for count2 in range(4):
        artist.forward(length + 52)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
         
    for count3 in range(4):
        artist.forward(length + 52)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count4 in range(4):
        artist.forward(length + 53)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count5 in range(5):
        artist.forward(length + 54)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count6 in range(4):
        artist.forward(length + 55)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count7 in range(4):
        artist.forward(length + 56)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count8 in range(4):
        artist.forward(length + 57)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count9 in range(4):
        artist.forward(length + 58)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count10 in range(5):
        artist.forward(length + 59)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count11 in range(4):
        artist.forward(length + 60)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count12 in range(4):
        artist.forward(length + 61)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count13 in range(4):
        artist.forward(length + 62)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count14 in range(4):
        artist.forward(length + 63)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count15 in range(5):
        artist.forward(length + 64)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count16 in range(4):
        artist.forward(length + 65)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count17 in range(4):
        artist.forward(length + 66)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count18 in range(4):
        artist.forward(length + 67)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count19 in range(4):
        artist.forward(length + 68)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count20 in range(5):
        artist.forward(length + 69)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count21 in range(4):
        artist.forward(length + 70)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count22 in range(4):
        artist.forward(length + 71)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count23 in range(4):
        artist.forward(length + 72)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count24 in range(4):
        artist.forward(length + 73)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count25 in range(4):
        artist.forward(length + 74)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count26 in range(4):
        artist.forward(length + 75)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count27 in range(4):
        artist.forward(length + 76)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count28 in range(4):
        artist.forward(length + 77)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count29 in range(4):
        artist.forward(length + 78)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count30 in range(4):
        artist.forward(length + 79)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count31 in range(4):
        artist.forward(length + 80)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count32 in range(4):
        artist.forward(length + 81)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count33 in range(4):
        artist.forward(length + 82)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count34 in range(4):
        artist.forward(length + 83)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count35 in range(5):
        artist.forward(length + 84)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count36 in range(4):
        artist.forward(length + 85)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count37 in range(4):
        artist.forward(length + 86)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count38 in range(4):
        artist.forward(length + 87)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count39 in range(4):
        artist.forward(length + 88)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count40 in range(5):
        artist.forward(length + 89)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count41 in range(4):
        artist.forward(length + 90)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count42 in range(4):
        artist.forward(length + 91)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count43 in range(4):
        artist.forward(length + 92)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count44 in range(4):
        artist.forward(length + 93)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count45 in range(4):
        artist.forward(length + 94)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count46 in range(5):
        artist.forward(length + 95)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count47 in range(4):
        artist.forward(length + 96)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count48 in range(4):
        artist.forward(length + 97)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count49 in range(4):
        artist.forward(length + 98)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count50 in range(4):
        artist.forward(length + 99)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count51 in range(5):
        artist.forward(length + 100)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count35 in range(5):
        artist.forward(length + 101)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count36 in range(4):
        artist.forward(length + 102)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count37 in range(4):
        artist.forward(length + 103)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count38 in range(4):
        artist.forward(length + 104)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count39 in range(4):
        artist.forward(length + 105)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count40 in range(5):
        artist.forward(length + 106)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count41 in range(4):
        artist.forward(length + 107)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count42 in range(4):
        artist.forward(length + 108)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count43 in range(4):
        artist.forward(length + 109)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count44 in range(4):
        artist.forward(length + 110)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count45 in range(4):
        artist.forward(length + 111)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count46 in range(5):
        artist.forward(length + 112)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count47 in range(4):
        artist.forward(length + 113)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count48 in range(4):
        artist.forward(length + 114)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count49 in range(4):
        artist.forward(length + 115)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count50 in range(4):
        artist.forward(length + 116)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random())
    for count51 in range(5):
        artist.forward(length + 117)
        artist.right(90)
        artist.pencolor(random.random(),random.random(),random.random()) 





pyramid(1)
