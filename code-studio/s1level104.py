'''s1level104
'''


import turtle
import random

artist = turtle.Turtle()
artist.pensize(2)
artist.speed(100)

def triangle_thing(length):
    for count in range(3):
        artist.forward(length)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count in range(3):
        artist.forward(length + 1)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count3 in range(3):
        artist.forward(length + 2)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random()) 
    for count4 in range(3):
        artist.forward(length + 3)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count5 in range(3):
        artist.forward(length + 4)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count6 in range(3):
        artist.forward(length + 5)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count7 in range(3):
        artist.forward(length + 6)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count8 in range(3):
        artist.forward(length + 7)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count9 in range(3):
        artist.forward(length + 8)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count10 in range(3):
        artist.forward(length + 9)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count11 in range(3):
        artist.forward(length + 10)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count12 in range(3):
        artist.forward(length + 11)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count13 in range(3):
        artist.forward(length + 12)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random()) 
    for count14 in range(3):
        artist.forward(length + 13)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count15 in range(3):
        artist.forward(length + 14)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count16 in range(3):
        artist.forward(length + 15)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count17 in range(3):
        artist.forward(length + 16)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count18 in range(3):
        artist.forward(length + 17)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count19 in range(3):
        artist.forward(length + 18)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count20 in range(3):
        artist.forward(length + 19)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count21 in range(3):
        artist.forward(length + 20)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count22 in range(3):
        artist.forward(length + 21)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count23 in range(3):
        artist.forward(length + 22)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random()) 
    for count24 in range(3):
        artist.forward(length + 23)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count25 in range(3):
        artist.forward(length + 24)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count26 in range(3):
        artist.forward(length + 25)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count27 in range(3):
        artist.forward(length + 26)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count28 in range(3):
        artist.forward(length + 27)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count29 in range(3):
        artist.forward(length + 28)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count30 in range(3):
        artist.forward(length + 29)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count41 in range(3):
        artist.forward(length + 30)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count42 in range(3):
        artist.forward(length + 31)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count43 in range(3):
        artist.forward(length + 32)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random()) 
    for count44 in range(3):
        artist.forward(length + 33)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count45 in range(3):
        artist.forward(length + 34)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count46 in range(3):
        artist.forward(length + 35)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count47 in range(3):
        artist.forward(length + 36)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count48 in range(3):
        artist.forward(length + 37)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count49 in range(3):
        artist.forward(length + 38)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count50 in range(3):
        artist.forward(length + 39)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count51 in range(3):
        artist.forward(length + 40)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count52 in range(3):
        artist.forward(length + 41)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count53 in range(3):
        artist.forward(length + 42)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random()) 
    for count54 in range(3):
        artist.forward(length + 43)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count55 in range(3):
        artist.forward(length + 44)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count56 in range(3):
        artist.forward(length + 45)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count57 in range(3):
        artist.forward(length + 46)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count58 in range(3):
        artist.forward(length + 47)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count59 in range(3):
        artist.forward(length + 48)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count60 in range(3):
        artist.forward(length + 49)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count61 in range(3):
        artist.forward(length + 50)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count62 in range(3):
        artist.forward(length + 51)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count63 in range(3):
        artist.forward(length + 52)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random()) 
    for count64 in range(3):
        artist.forward(length + 53)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count65 in range(3):
        artist.forward(length + 54)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count66 in range(3):
        artist.forward(length + 55)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count67 in range(3):
        artist.forward(length + 56)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count68 in range(3):
        artist.forward(length + 57)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count69 in range(3):
        artist.forward(length + 58)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count70 in range(3):
        artist.forward(length + 59)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count71 in range(3):
        artist.forward(length + 60)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count72 in range(3):
        artist.forward(length + 61)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count73 in range(3):
        artist.forward(length + 62)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random()) 
    for count74 in range(3):
        artist.forward(length + 63)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count75 in range(3):
        artist.forward(length + 64)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count76 in range(3):
        artist.forward(length + 65)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count77 in range(3):
        artist.forward(length + 66)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count78 in range(3):
        artist.forward(length + 67)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count79 in range(3):
        artist.forward(length + 68)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count80 in range(3):
        artist.forward(length + 69)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count81 in range(3):
        artist.forward(length + 70)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count82 in range(3):
        artist.forward(length + 71)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count83 in range(3):
        artist.forward(length + 72)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random()) 
    for count84 in range(3):
        artist.forward(length + 73)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count85 in range(3):
        artist.forward(length + 74)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count86 in range(3):
        artist.forward(length + 75)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count87 in range(3):
        artist.forward(length + 76)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count88 in range(3):
        artist.forward(length + 77)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count89 in range(3):
        artist.forward(length + 78)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count100 in range(3):
        artist.forward(length + 79)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count in range(3):
        artist.forward(length + 80)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count in range(3):
        artist.forward(length + 81)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count3 in range(3):
        artist.forward(length + 82)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random()) 
    for count4 in range(3):
        artist.forward(length + 83)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count5 in range(3):
        artist.forward(length + 84)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count6 in range(3):
        artist.forward(length + 85)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count7 in range(3):
        artist.forward(length + 86)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count8 in range(3):
        artist.forward(length + 87)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count9 in range(3):
        artist.forward(length + 88)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count10 in range(3):
        artist.forward(length + 89)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count11 in range(3):
        artist.forward(length + 90)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count12 in range(3):
        artist.forward(length + 91)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count13 in range(3):
        artist.forward(length + 92)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random()) 
    for count14 in range(3):
        artist.forward(length + 93)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count15 in range(3):
        artist.forward(length + 94)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count16 in range(3):
        artist.forward(length + 95)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count17 in range(3):
        artist.forward(length + 96)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count18 in range(3):
        artist.forward(length + 97)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count19 in range(3):
        artist.forward(length + 98)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count20 in range(3):
        artist.forward(length + 99)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count21 in range(3):
        artist.forward(length + 100)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count22 in range(3):
        artist.forward(length + 101)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count23 in range(3):
        artist.forward(length + 102)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random()) 
    for count24 in range(3):
        artist.forward(length + 103)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count25 in range(3):
        artist.forward(length + 104)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count26 in range(3):
        artist.forward(length + 105)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count27 in range(3):
        artist.forward(length + 106)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count28 in range(3):
        artist.forward(length + 107)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count29 in range(3):
        artist.forward(length + 108)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count30 in range(3):
        artist.forward(length + 109)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count41 in range(3):
        artist.forward(length + 110)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count42 in range(3):
        artist.forward(length + 111)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count43 in range(3):
        artist.forward(length + 112)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random()) 
    for count44 in range(3):
        artist.forward(length + 113)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count45 in range(3):
        artist.forward(length + 114)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count46 in range(3):
        artist.forward(length + 115)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count47 in range(3):
        artist.forward(length + 116)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count48 in range(3):
        artist.forward(length + 117)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count49 in range(3):
        artist.forward(length + 118)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count50 in range(3):
        artist.forward(length + 119)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count51 in range(3):
        artist.forward(length + 120)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count52 in range(3):
        artist.forward(length + 121)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count53 in range(3):
        artist.forward(length + 122)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random()) 
    for count54 in range(3):
        artist.forward(length + 123)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count55 in range(3):
        artist.forward(length + 124)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count56 in range(3):
        artist.forward(length + 125)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count57 in range(3):
        artist.forward(length + 126)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count58 in range(3):
        artist.forward(length + 127)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count59 in range(3):
        artist.forward(length + 128)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count60 in range(3):
        artist.forward(length + 129)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count61 in range(3):
        artist.forward(length + 130)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count62 in range(3):
        artist.forward(length + 131)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count63 in range(3):
        artist.forward(length + 132)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random()) 
    for count64 in range(3):
        artist.forward(length + 133)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count65 in range(3):
        artist.forward(length + 134)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count66 in range(3):
        artist.forward(length + 135)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count67 in range(3):
        artist.forward(length + 136)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count68 in range(3):
        artist.forward(length + 137)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count69 in range(3):
        artist.forward(length + 138)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count70 in range(3):
        artist.forward(length + 139)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count71 in range(3):
        artist.forward(length + 140)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count72 in range(3):
        artist.forward(length + 141)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count73 in range(3):
        artist.forward(length + 142)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random()) 
    for count74 in range(3):
        artist.forward(length + 143)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count75 in range(3):
        artist.forward(length + 144)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count76 in range(3):
        artist.forward(length + 145)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count77 in range(3):
        artist.forward(length + 146)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count78 in range(3):
        artist.forward(length + 147)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count79 in range(3):
        artist.forward(length + 148)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count80 in range(3):
        artist.forward(length + 149)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count81 in range(3):
        artist.forward(length + 150)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count82 in range(3):
        artist.forward(length + 151)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count83 in range(3):
        artist.forward(length + 152)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random()) 
    for count84 in range(3):
        artist.forward(length + 153)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count85 in range(3):
        artist.forward(length + 154)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count86 in range(3):
        artist.forward(length + 155)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count87 in range(3):
        artist.forward(length + 156)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count88 in range(3):
        artist.forward(length + 157)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count89 in range(3):
        artist.forward(length + 158)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())
    for count100 in range(3):
        artist.forward(length + 159)
        artist.right(121)
        artist.pencolor(random.random(),random.random(),random.random())

triangle_thing(1)

    
