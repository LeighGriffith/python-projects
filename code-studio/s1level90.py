'''s1level90
var length2;
var counter;

function draw_a_square(length2) {
  for (var count = 0; count < 4; count++) {
    moveForward(length2);
    turnRight(90);
  }
}

function draw_a_triangle(length2) {
  for (var count2 = 0; count2 < 3; count2++) {
    moveForward(length2);

function draw_a_house(length2) {
   function draw_a_square(length2) {
  for (var count = 0; count < 4; count++) {
    moveForward(length2);
    turnRight(90);
  }
}

function draw_a_triangle(length2) {
  for (var count2 = 0; count2 < 3; count2++) {
    moveForward(length2);


from counter [length * 0.5, length * 0.7, length * 0.9]
   for (var degree = 0; degree < 90; degree++) {
      draw_a_house
      turn_right(60)
      move_forward(counter)
      turn_right(90)
      move_forward(counter)
      turn_left(90)
      }
      if counter =! 2) {
'''

import turtle

artist = turtle.Turtle()
artist.pensize(7)
artist.pencolor('black')
artist.speed(100)
artist.left(90)

def draw_a_square(length):
        for count in range(4): 
            artist.forward(length)
            artist.right(90)

def draw_a_triangle(length):
    for count in range(3):
        artist.forward(length)
        artist.right(120)


def draw_a_house(length):
    def draw_a_square(length):
        for count in range(4): 
            artist.forward(length)
            artist.right(90)
    def draw_a_triangle(length):
        for count in range(3):
            artist.forward(length)
            artist.right(120)
    draw_a_square(length)
    artist.forward(length)
    artist.right(30)
    draw_a_triangle(length)

 
draw_a_house(50)
artist.right(60)
artist.forward(50)
artist.right(90)
artist.forward(50)
artist.left(180)
draw_a_house(100)
artist.right(60)
artist.forward(100)
artist.right(90)
artist.forward(100)
artist.left(180)
draw_a_house(150)

   
   
    
